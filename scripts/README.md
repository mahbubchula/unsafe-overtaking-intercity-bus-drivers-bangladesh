# Repository Utilities

Validation scripts inspect repository structure and research-document consistency without changing scientific content. `sync_from_private.py` copies only files named in `github-allowlist.json`, defaults to a dry run, never deletes files, and does not commit or push.
