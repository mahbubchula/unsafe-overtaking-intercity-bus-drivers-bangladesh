# Thesis Appendices

Store LaTeX appendix source here when available. Preserve the labels `app:questionnaire` and `app:ethics_materials` and do not activate repository links before the referenced version is immutable.
