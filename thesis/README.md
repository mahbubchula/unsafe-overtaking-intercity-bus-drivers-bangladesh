# Thesis Source

This directory is reserved for the complete Master of Engineering thesis source. The existing LaTeX project was not present in the inspected workspace, so no `main.tex`, chapter, appendix, bibliography, class, style, figure, or compiled thesis file has been fabricated.

When the source is supplied, preserve its working layout unless a path migration can be completed and compiled safely. In particular:

- preserve citation keys and bibliography configuration;
- preserve chapter, section, equation, table, figure, and appendix labels;
- preserve the appendix labels `app:questionnaire` and `app:ethics_materials`;
- keep repository links pending until the owner and immutable questionnaire version are confirmed;
- retain class files, style files, custom commands, and build instructions;
- do not commit a compiled thesis PDF unless its status and disclosure review are confirmed.

Expected target locations include `latex/main.tex`, `latex/chapters/`, `latex/appendices/`, `latex/frontmatter/`, `latex/bibliography/`, `latex/figures/`, and `compiled/`. These targets do not override a functioning source layout.
