# Machine Learning

This directory is reserved for supervised prediction of unsafe-overtaking occurrence using regularized logistic regression, Random Forest, XGBoost, and CatBoost. Preprocessing, tuning, and feature inclusion must occur inside the appropriate resampling boundaries.

S1 and S2 are exploratory safety-history measures and are not primary machine-learning predictors. No trained models or performance results are included.
